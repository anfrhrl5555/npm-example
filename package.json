{
  "name": "sample-vite-app",
  "version": "1.0.0",
  "description": "Sample frontend app with Vite (build-only mode)",
  "scripts": {
    "build": "vite build --mode production",
    "build:dev": "vite build --mode development"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
