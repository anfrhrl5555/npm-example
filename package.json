{
  "name": "sample-vite-app",
  "version": "1.0.0",
  "description": "Sample frontend app with Vite (build-only)",
  "scripts": {
    "dev": "vite build --mode development",
    "prod": "vite build --mode production",
    "build": "vite build --mode production"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
